pipeline {
    agent any

    stages {
        stage('Checkout Test') {
            steps {
                echo 'Repository fetched successfully'
            }
        }

        stage('Pipeline Test') {
            steps {
                echo 'Jenkins Pipeline is working'
            }
        }
    }
}
